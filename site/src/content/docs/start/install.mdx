---
title: Install
description: Install the QA My App (qa-my-app) Claude Code plugin from its self-marketplace.
---

import { Steps, Aside } from '@astrojs/starlight/components';

Inside Claude Code, in any project:

```text
/plugin marketplace add elwizard33/qa-my-app
/plugin install qa-my-app@qa-my-app
/reload-plugins
```

That's it — the plugin is installed at **user scope** (available in every repo) and active in your current session.

<Aside type="caution" title="Upgrading from 0.1.0?">
The plugin was renamed `qa-catalog` → `qa-my-app` in v1.0.0, so this is a reinstall rather than an update:

```text
/plugin uninstall qa-catalog@qa-my-app
/plugin install qa-my-app@qa-my-app
```

Every command moves from `/qa-catalog:*` to `/qa-my-app:*`. Your generated `QA-tests/` directory is **untouched** — catalog, fingerprints, results history, and `auth.local.json` all keep working, so there's no need to re-run `init`.
</Aside>

## What the three commands do

<Steps>

1. **`/plugin marketplace add elwizard33/qa-my-app`** registers this repo's `marketplace.json` with your Claude Code install. No plugins are installed yet — Claude Code just learns the catalog exists. ([docs](https://code.claude.com/docs/en/discover-plugins#add-marketplaces))

2. **`/plugin install qa-my-app@qa-my-app`** installs the `qa-my-app` plugin from the `qa-my-app` marketplace at **user scope** by default. To install at project scope so teammates pick it up automatically, run `/plugin` instead and press Enter on **qa-my-app** in the Discover tab — you'll get scope options. ([docs](https://code.claude.com/docs/en/discover-plugins#install-plugins))

3. **`/reload-plugins`** activates the new plugin without restarting Claude Code.

</Steps>

Verify it loaded by opening the plugin manager with `/plugin` and looking at the **Installed** tab — you should see **QA My App** with skills `init`, `scan`, `sync`, `status`, `run`, `run-all`, `verify`. Errors (if any) show up under the **Errors** tab.

<Aside type="note" title="Namespacing">
Slash commands are exposed under the `qa-my-app:` namespace — e.g. `/qa-my-app:run-all`. As of v1.0.0 the namespace, the plugin id, the marketplace, and the brand are all the same word.

Two of the seven don't need typing at all: `status` and `verify` are model-invocable, so asking "is QA set up?" or "verify what I changed" reaches them directly. The other five only run when you type them — see [Commands](/qa-my-app/reference/commands/).
</Aside>

## Local development / contributing

If you've cloned the repo and want to test changes without publishing, launch Claude Code with the `--plugin-dir` flag instead:

```bash
claude --plugin-dir /path/to/qa-my-app
```

The local copy takes precedence over any installed marketplace version for that session. ([docs](https://code.claude.com/docs/en/plugins#test-your-plugins-locally))

<Aside type="tip" title="Browser engine">
The plugin is browser-engine agnostic and ships **no session-level MCP server**. The project-level browser agents declare their **own** inline `mcpServers` block, so every parallel spawn gets a dedicated browser process and the main conversation never pays for the browser tool descriptions. Set `browser_engine` to `playwright` (default), `chrome-devtools`, or `stagehand`. See [Browser engines](/qa-my-app/browsers/).
</Aside>
